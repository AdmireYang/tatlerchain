# Port Magazine 开发流程文档

> 📌 本文档详细说明从零开始搭建和开发 Port Magazine 项目的完整流程

---

## 📋 目录

1. [环境准备](#1-环境准备)
2. [项目初始化](#2-项目初始化)
3. [数据库配置](#3-数据库配置)
4. [开发流程](#4-开发流程)
5. [功能开发顺序](#5-功能开发顺序)
6. [测试流程](#6-测试流程)
7. [部署流程](#7-部署流程)

---

## 1. 环境准备

### 1.1 确认已安装必需软件

参考 [本地环境安装清单.md](./本地环境安装清单.md)，确保已安装：

- ✅ Node.js (>= 18.0.0)
- ✅ pnpm (>= 8.0.0)
- ✅ Git
- ✅ VS Code
- ✅ PostgreSQL 或 Docker
- ⚠️ Redis (可选)

### 1.2 验证环境

```bash
# 验证 Node.js
node -v

# 验证 pnpm
pnpm -v

# 验证 Git
git --version

# 验证 Docker (如果使用)
docker --version
```

---

## 2. 项目初始化

### 2.1 克隆或初始化项目

**如果是新项目**（当前情况）：

```bash
# 项目目录已创建，直接进入
cd port

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 首次提交
git commit -m "feat: 初始化 Port Magazine 项目"
```

**如果是克隆现有项目**：

```bash
git clone <项目地址>
cd port
```

### 2.2 安装依赖

```bash
# 安装所有依赖（根目录和所有子包）
pnpm install
```

这个命令会：

- 安装根目录的依赖
- 安装 `apps/web`、`apps/admin`、`apps/api` 的依赖
- 安装 `packages/*` 的依赖
- 建立包之间的链接关系

### 2.3 配置环境变量

```bash
# 复制环境变量模板
cp apps/web/.env.example apps/web/.env
cp apps/admin/.env.example apps/admin/.env
cp apps/api/.env.example apps/api/.env
cp packages/database/.env.example packages/database/.env
```

**编辑 `packages/database/.env`**：

```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/port_db?schema=public"
```

**编辑 `apps/api/.env`**：

```env
PORT=3001
DATABASE_URL="postgresql://postgres:password@localhost:5432/port_db"
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
```

---

## 3. 数据库配置

### 3.1 启动数据库（使用 Docker）

```bash
# 启动 PostgreSQL 和 Redis
docker-compose up -d

# 查看容器状态
docker ps

# 查看日志
docker-compose logs -f postgres
```

**容器说明**：

- `port-postgres`: PostgreSQL 数据库 (端口 5432)
- `port-redis`: Redis 缓存 (端口 6379)
- `port-pgadmin`: pgAdmin 管理工具 (端口 5050)

### 3.2 运行数据库迁移

```bash
# 进入 database 包目录
cd packages/database

# 生成 Prisma Client
pnpm db:generate

# 运行数据库迁移
pnpm db:migrate

# 返回根目录
cd ../..
```

### 3.3 查看数据库（可选）

**方式一：使用 Prisma Studio**

```bash
cd packages/database
pnpm db:studio
```

访问：http://localhost:5555

**方式二：使用 pgAdmin**
访问：http://localhost:5050

- 邮箱：admin@port.com
- 密码：admin

---

## 4. 开发流程

### 4.1 启动开发服务器

**方式一：同时启动所有服务**

```bash
# 在根目录执行
pnpm dev
```

这会同时启动：

- 前台应用 (http://localhost:3000)
- 后台管理 (http://localhost:3002)
- API 服务 (http://localhost:3001)

**方式二：分别启动服务**

```bash
# 终端 1：启动 API 服务
pnpm api:dev

# 终端 2：启动前台应用
pnpm web:dev

# 终端 3：启动后台管理
pnpm admin:dev
```

### 4.2 访问应用

- **前台应用**: http://localhost:3000
- **后台管理**: http://localhost:3002
- **API 服务**: http://localhost:3001/api
- **API 健康检查**: http://localhost:3001/api/health

### 4.3 开发规范

#### 代码规范

```bash
# 运行 ESLint 检查
pnpm lint

# 格式化代码（如果配置了 Prettier）
pnpm format
```

#### Git 提交规范

使用 Conventional Commits 规范：

```bash
# 功能开发
git commit -m "feat: 添加用户登录功能"

# Bug 修复
git commit -m "fix: 修复推文列表分页问题"

# 文档更新
git commit -m "docs: 更新 API 文档"

# 样式调整
git commit -m "style: 调整按钮样式"

# 重构
git commit -m "refactor: 重构推文编辑器组件"

# 测试
git commit -m "test: 添加用户认证测试"

# 构建相关
git commit -m "build: 更新依赖版本"
```

#### 分支管理

```bash
# 创建功能分支
git checkout -b feature/user-auth

# 创建修复分支
git checkout -b fix/post-pagination

# 开发完成后合并到 develop
git checkout develop
git merge feature/user-auth

# 删除已合并的分支
git branch -d feature/user-auth
```

---

## 5. 功能开发顺序

### 阶段一：基础架构（第 1-2 周）

#### 1.1 后端 API 基础

- [ ] 配置 Nest.js 项目结构
- [ ] 集成 Prisma ORM
- [ ] 实现 JWT 认证中间件
- [ ] 创建基础 CRUD 模块

**开发步骤**：

```bash
# 1. 创建认证模块
cd apps/api
nest g module auth
nest g service auth
nest g controller auth

# 2. 创建用户模块
nest g module users
nest g service users
nest g controller users

# 3. 创建推文模块
nest g module posts
nest g service posts
nest g controller posts

# 4. 创建广告模块
nest g module ads
nest g service ads
nest g controller ads
```

#### 1.2 前端基础组件

- [ ] 搭建前台布局（Header、Footer）
- [ ] 搭建后台布局（Sidebar、TopBar）
- [ ] 创建共享 UI 组件
- [ ] 配置路由和状态管理

**开发步骤**：

```bash
# 前台应用
cd apps/web
# 创建布局组件
mkdir -p layouts components/common

# 后台应用
cd apps/admin
# 创建布局和页面组件
mkdir -p src/layouts src/components src/stores
```

### 阶段二：核心功能（第 3-5 周）

#### 2.1 用户认证系统

- [ ] 后端：实现登录/登出 API
- [ ] 后端：实现 JWT Token 验证
- [ ] 前端：实现登录页面
- [ ] 前端：实现认证状态管理
- [ ] 前端：实现路由守卫

**API 接口**：

```typescript
// apps/api/src/auth/auth.controller.ts
@Post('login')
async login(@Body() loginDto: LoginDto) {
  // 实现登录逻辑
}

@Post('logout')
async logout() {
  // 实现登出逻辑
}

@Get('me')
@UseGuards(JwtAuthGuard)
async getProfile(@Request() req) {
  // 获取当前用户信息
}
```

**前端实现**：

```vue
<!-- apps/admin/src/pages/Login.vue -->
<template>
  <el-form @submit="handleLogin">
    <el-form-item label="邮箱">
      <el-input v-model="form.email" />
    </el-form-item>
    <el-form-item label="密码">
      <el-input v-model="form.password" type="password" />
    </el-form-item>
    <el-button type="primary" @click="handleLogin">登录</el-button>
  </el-form>
</template>
```

#### 2.2 推文管理系统

- [ ] 后端：推文 CRUD API
- [ ] 后端：推文状态管理（草稿/发布/归档）
- [ ] 后端：推文搜索和筛选
- [ ] 前端：推文列表页面
- [ ] 前端：推文编辑器（富文本）
- [ ] 前端：推文详情页面

**数据流程**：

```
1. 创建推文 → 保存为草稿
2. 编辑推文 → 更新内容
3. 发布推文 → 状态改为已发布
4. 前台展示 → 只显示已发布的推文
```

#### 2.3 广告管理系统

- [ ] 后端：广告 CRUD API
- [ ] 后端：广告位置管理
- [ ] 后端：广告统计（点击/展示）
- [ ] 前端：广告列表页面
- [ ] 前端：广告创建/编辑表单
- [ ] 前台：广告展示组件

### 阶段三：数据看板（第 6 周）

#### 3.1 统计数据 API

- [ ] 推文统计（总数、浏览量、发布趋势）
- [ ] 广告统计（点击率、展示次数）
- [ ] 用户活动统计

#### 3.2 数据可视化

- [ ] 集成图表库（ECharts 或 Chart.js）
- [ ] 实现推文看板
- [ ] 实现广告看板
- [ ] 实现数据导出功能

### 阶段四：优化和完善（第 7-8 周）

#### 4.1 性能优化

- [ ] 实现 Redis 缓存
- [ ] 图片懒加载
- [ ] 代码分割和懒加载
- [ ] API 响应压缩

#### 4.2 用户体验优化

- [ ] 加载状态提示
- [ ] 错误处理和提示
- [ ] 表单验证优化
- [ ] 响应式设计调整

#### 4.3 安全加固

- [ ] XSS 防护
- [ ] CSRF 防护
- [ ] 文件上传安全
- [ ] API 限流

---

## 6. 测试流程

### 6.1 单元测试

```bash
# 运行所有测试
pnpm test

# 运行特定包的测试
pnpm --filter @port/api test
pnpm --filter @port/web test
```

### 6.2 E2E 测试

```bash
# 安装 Playwright
pnpm add -D @playwright/test

# 运行 E2E 测试
pnpm test:e2e
```

### 6.3 手动测试清单

#### 后台管理测试

- [ ] 登录/登出功能
- [ ] 推文创建/编辑/删除
- [ ] 推文发布/归档
- [ ] 广告创建/编辑/删除
- [ ] 数据看板显示

#### 前台展示测试

- [ ] 首页推文列表加载
- [ ] 推文详情页显示
- [ ] 广告正确展示
- [ ] 响应式布局

---

## 7. 部署流程

### 7.1 构建生产版本

```bash
# 构建所有应用
pnpm build

# 或分别构建
pnpm --filter @port/web build
pnpm --filter @port/admin build
pnpm --filter @port/api build
```

### 7.2 Docker 部署

**创建 Dockerfile**：

```dockerfile
# apps/api/Dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install --frozen-lockfile
COPY . .
RUN pnpm build
CMD ["pnpm", "start:prod"]
```

**构建和运行**：

```bash
# 构建镜像
docker build -t port-api ./apps/api

# 运行容器
docker run -p 3001:3001 port-api
```

### 7.3 Vercel 部署（前端）

**前台应用**：

```bash
cd apps/web
vercel
```

**后台应用**：

```bash
cd apps/admin
vercel
```

### 7.4 生产环境检查清单

- [ ] 环境变量已配置
- [ ] 数据库迁移已运行
- [ ] JWT Secret 已更改
- [ ] CORS 配置正确
- [ ] 日志系统已配置
- [ ] 监控系统已配置
- [ ] 备份策略已制定

---

## 8. 常见问题

### Q1: pnpm install 失败？

```bash
# 清除缓存
pnpm store prune

# 删除 node_modules 和 lock 文件
rm -rf node_modules pnpm-lock.yaml

# 重新安装
pnpm install
```

### Q2: 数据库连接失败？

```bash
# 检查 Docker 容器状态
docker ps

# 重启容器
docker-compose restart postgres

# 查看日志
docker-compose logs postgres
```

### Q3: 端口被占用？

```bash
# Windows 查看端口占用
netstat -ano | findstr :3000

# macOS/Linux 查看端口占用
lsof -i :3000

# 杀死进程
kill -9 <PID>
```

### Q4: Prisma 迁移失败？

```bash
# 重置数据库
cd packages/database
pnpm prisma migrate reset

# 重新运行迁移
pnpm db:migrate
```

---

## 9. 开发技巧

### 9.1 快速创建新组件

```bash
# 前台组件
cd apps/web/components
touch NewComponent.vue

# 后台组件
cd apps/admin/src/components
touch NewComponent.vue
```

### 9.2 调试技巧

**后端调试**：

```typescript
// 使用 console.log
console.log('Debug:', data)

// 使用 Logger
import { Logger } from '@nestjs/common'
const logger = new Logger('ClassName')
logger.debug('Debug message')
```

**前端调试**：

```vue
<script setup>
// 使用 console.log
console.log('Data:', data)

// 使用 Vue DevTools
// 安装浏览器扩展后可以查看组件状态
</script>
```

### 9.3 热重载

所有应用都支持热重载，修改代码后会自动刷新：

- **Nuxt 3**: 自动热重载
- **Vite**: 自动热重载
- **Nest.js**: 使用 `--watch` 参数自动重启

---

## 10. 下一步

完成基础搭建后，建议按以下顺序开发：

1. ✅ **第一步**：完成用户认证系统
2. ✅ **第二步**：实现推文管理功能
3. ✅ **第三步**：实现广告管理功能
4. ✅ **第四步**：完善数据看板
5. ✅ **第五步**：优化和测试

---

## 📚 相关文档

- [需求文档](./需求文档.md)
- [设计方案](./设计方案.md)
- [本地环境安装清单](./本地环境安装清单.md)

---

## ✅ 总结

按照本文档的流程，你可以：

1. 快速搭建开发环境
2. 理解项目结构和开发规范
3. 按阶段完成功能开发
4. 进行测试和部署

祝你开发顺利！🎉

# Port Magazine 本地开发环境安装清单

> 📌 本文档适合开发小白，每一步都有详细说明

---

## 📋 安装清单概览

| 序号 | 软件/工具 | 用途 | 必需性 |
|------|----------|------|--------|
| 1 | Node.js | JavaScript 运行环境 | ✅ 必需 |
| 2 | pnpm | 包管理工具 | ✅ 必需 |
| 3 | Git | 版本控制工具 | ✅ 必需 |
| 4 | VS Code | 代码编辑器 | ✅ 推荐 |
| 5 | PostgreSQL | 数据库 | ✅ 必需 |
| 6 | Redis | 缓存数据库 | ⚠️ 可选 |
| 7 | Docker Desktop | 容器化工具 | ⚠️ 推荐 |

---

## 1️⃣ 安装 Node.js

### 什么是 Node.js？
Node.js 是 JavaScript 的运行环境，让 JavaScript 可以在服务器端运行。

### 安装步骤

#### Windows 系统：
1. 访问官网：https://nodejs.org/
2. 下载 **LTS 版本**（长期支持版，更稳定）
3. 双击下载的 `.msi` 文件
4. 一路点击 "Next"，保持默认选项
5. 安装完成后，重启电脑

#### macOS 系统：
```bash
# 使用 Homebrew 安装（推荐）
brew install node

# 或者从官网下载 .pkg 文件安装
# https://nodejs.org/
```

#### Linux 系统：
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_lts.x | sudo bash -
sudo yum install -y nodejs
```

### 验证安装
打开命令行（Windows 按 `Win + R`，输入 `cmd`），输入：
```bash
node -v
# 应该显示版本号，例如：v20.11.0

npm -v
# 应该显示版本号，例如：10.2.4
```

✅ **推荐版本**：Node.js 18.x 或 20.x（LTS 版本）

---

## 2️⃣ 安装 pnpm

### 什么是 pnpm？
pnpm 是一个快速、节省磁盘空间的包管理工具，比 npm 更高效。

### 安装步骤

#### 方式一：使用 npm 安装（推荐）
```bash
npm install -g pnpm
```

#### 方式二：使用安装脚本
**Windows (PowerShell)**：
```powershell
iwr https://get.pnpm.io/install.ps1 -useb | iex
```

**macOS/Linux**：
```bash
curl -fsSL https://get.pnpm.io/install.sh | sh -
```

### 验证安装
```bash
pnpm -v
# 应该显示版本号，例如：8.15.0
```

### 配置 pnpm（可选但推荐）
```bash
# 设置国内镜像源（加速下载）
pnpm config set registry https://registry.npmmirror.com
```

---

## 3️⃣ 安装 Git

### 什么是 Git？
Git 是版本控制工具，用于管理代码的历史记录和团队协作。

### 安装步骤

#### Windows 系统：
1. 访问官网：https://git-scm.com/download/win
2. 下载安装包
3. 双击安装，**重要选项**：
   - 选择 "Use Git from the Windows Command Prompt"
   - 选择 "Checkout Windows-style, commit Unix-style line endings"
   - 其他保持默认

#### macOS 系统：
```bash
# 使用 Homebrew 安装
brew install git

# 或者从官网下载安装
# https://git-scm.com/download/mac
```

#### Linux 系统：
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install git

# CentOS/RHEL
sudo yum install git
```

### 验证安装
```bash
git --version
# 应该显示版本号，例如：git version 2.43.0
```

### 配置 Git（首次使用必须配置）
```bash
# 设置用户名
git config --global user.name "你的名字"

# 设置邮箱
git config --global user.email "your.email@example.com"

# 查看配置
git config --list
```

---

## 4️⃣ 安装 VS Code（代码编辑器）

### 什么是 VS Code？
Visual Studio Code 是微软开发的免费代码编辑器，功能强大且易用。

### 安装步骤

#### 所有系统：
1. 访问官网：https://code.visualstudio.com/
2. 下载对应系统的安装包
3. 安装（Windows 双击 `.exe`，macOS 拖到应用程序文件夹）

### 推荐安装的 VS Code 插件

安装方法：打开 VS Code，点击左侧扩展图标（或按 `Ctrl+Shift+X`），搜索并安装：

1. **Vue Language Features (Volar)** - Vue 3 支持
2. **TypeScript Vue Plugin (Volar)** - Vue 中的 TypeScript 支持
3. **ESLint** - 代码规范检查
4. **Prettier** - 代码格式化
5. **Tailwind CSS IntelliSense** - Tailwind CSS 智能提示
6. **GitLens** - Git 增强工具
7. **Path Intellisense** - 路径智能提示
8. **Auto Rename Tag** - 自动重命名标签
9. **Chinese (Simplified)** - 中文语言包（可选）

### 安装插件步骤：
```
1. 打开 VS Code
2. 点击左侧扩展图标（四个方块）
3. 搜索插件名称
4. 点击 "Install" 安装
```

---

## 5️⃣ 安装 PostgreSQL（数据库）

### 什么是 PostgreSQL？
PostgreSQL 是一个强大的开源关系型数据库。

### 方式一：直接安装（适合初学者）

#### Windows 系统：
1. 访问官网：https://www.postgresql.org/download/windows/
2. 下载 PostgreSQL 安装包（推荐版本 15 或 16）
3. 双击安装，**重要设置**：
   - 设置超级用户密码（记住这个密码！）
   - 端口号保持默认 `5432`
   - 勾选 "pgAdmin 4"（数据库管理工具）

#### macOS 系统：
```bash
# 使用 Homebrew 安装
brew install postgresql@15

# 启动服务
brew services start postgresql@15
```

#### Linux 系统：
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install postgresql postgresql-contrib

# 启动服务
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### 方式二：使用 Docker（推荐，更简单）

**前提**：需要先安装 Docker Desktop（见第 7 步）

```bash
# 拉取 PostgreSQL 镜像并运行
docker run --name port-postgres \
  -e POSTGRES_PASSWORD=your_password \
  -e POSTGRES_DB=port_db \
  -p 5432:5432 \
  -d postgres:15
```

### 验证安装
```bash
# 方式一：直接安装的验证
psql --version
# 应该显示版本号，例如：psql (PostgreSQL) 15.5

# 方式二：Docker 安装的验证
docker ps
# 应该看到 port-postgres 容器在运行
```

### 创建数据库（直接安装方式）
```bash
# Windows: 打开 "SQL Shell (psql)"
# macOS/Linux: 打开终端

# 连接到 PostgreSQL
psql -U postgres

# 创建数据库
CREATE DATABASE port_db;

# 退出
\q
```

---

## 6️⃣ 安装 Redis（可选）

### 什么是 Redis？
Redis 是一个内存数据库，用于缓存，提升应用性能。

### 方式一：使用 Docker（推荐）

```bash
# 拉取 Redis 镜像并运行
docker run --name port-redis \
  -p 6379:6379 \
  -d redis:7
```

### 方式二：直接安装

#### Windows 系统：
1. 下载：https://github.com/tporadowski/redis/releases
2. 解压到 `C:\Redis`
3. 双击 `redis-server.exe` 启动

#### macOS 系统：
```bash
brew install redis
brew services start redis
```

#### Linux 系统：
```bash
# Ubuntu/Debian
sudo apt-get install redis-server
sudo systemctl start redis
```

### 验证安装
```bash
# 方式一：直接安装
redis-cli ping
# 应该返回：PONG

# 方式二：Docker
docker exec -it port-redis redis-cli ping
# 应该返回：PONG
```

---

## 7️⃣ 安装 Docker Desktop（推荐）

### 什么是 Docker？
Docker 可以将应用和依赖打包成容器，简化开发环境配置。

### 安装步骤

#### Windows 系统：
1. 访问官网：https://www.docker.com/products/docker-desktop/
2. 下载 Docker Desktop for Windows
3. 双击安装
4. **重要**：安装过程中会要求启用 WSL 2，按提示操作
5. 安装完成后重启电脑

#### macOS 系统：
1. 访问官网：https://www.docker.com/products/docker-desktop/
2. 下载 Docker Desktop for Mac（区分 Intel 和 Apple Silicon）
3. 拖到应用程序文件夹
4. 打开 Docker Desktop

#### Linux 系统：
```bash
# Ubuntu
sudo apt-get update
sudo apt-get install docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker

# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER
```

### 验证安装
```bash
docker --version
# 应该显示版本号，例如：Docker version 24.0.7

docker-compose --version
# 应该显示版本号，例如：Docker Compose version v2.23.0
```

---

## 8️⃣ 其他推荐工具

### 8.1 Postman（API 测试工具）
- 官网：https://www.postman.com/downloads/
- 用途：测试后端 API 接口

### 8.2 TablePlus / DBeaver（数据库管理工具）
- TablePlus：https://tableplus.com/ （付费，界面美观）
- DBeaver：https://dbeaver.io/ （免费，功能强大）
- 用途：可视化管理数据库

### 8.3 Chrome / Edge 浏览器
- 用途：前端开发和调试
- 推荐安装 Vue DevTools 扩展

---

## 📝 安装完成检查清单

完成所有安装后，运行以下命令检查：

```bash
# 检查 Node.js
node -v

# 检查 npm
npm -v

# 检查 pnpm
pnpm -v

# 检查 Git
git --version

# 检查 PostgreSQL（直接安装）
psql --version

# 检查 Docker
docker --version

# 检查 Redis（如果安装了）
redis-cli --version
```

---

## 🚀 下一步

安装完成后，你可以：

1. **克隆项目**（如果已有代码仓库）：
   ```bash
   git clone <项目地址>
   cd port
   ```

2. **初始化项目**（如果是新项目）：
   ```bash
   mkdir port
   cd port
   pnpm init
   ```

3. **参考设计方案文档**，开始搭建项目结构

---

## ❓ 常见问题

### Q1: 命令行在哪里打开？
- **Windows**：按 `Win + R`，输入 `cmd` 或 `powershell`
- **macOS**：按 `Cmd + Space`，输入 `Terminal`
- **Linux**：按 `Ctrl + Alt + T`

### Q2: 如何知道软件是否安装成功？
运行对应的 `--version` 或 `-v` 命令，如果显示版本号就说明安装成功。

### Q3: 安装过程中遇到权限问题怎么办？
- **Windows**：右键选择"以管理员身份运行"
- **macOS/Linux**：命令前加 `sudo`

### Q4: npm 或 pnpm 下载很慢怎么办？
使用国内镜像源：
```bash
# npm
npm config set registry https://registry.npmmirror.com

# pnpm
pnpm config set registry https://registry.npmmirror.com
```

### Q5: Docker Desktop 启动失败？
- Windows：确保已启用 WSL 2 和虚拟化
- macOS：检查系统版本是否支持
- 重启电脑后再试

---

## 📚 学习资源

### 官方文档
- Node.js：https://nodejs.org/docs/
- Vue 3：https://cn.vuejs.org/
- Nuxt 3：https://nuxt.com/
- TypeScript：https://www.typescriptlang.org/zh/
- Tailwind CSS：https://tailwindcss.com/

### 视频教程
- B站搜索：Vue 3 入门教程
- B站搜索：TypeScript 入门教程
- B站搜索：Git 使用教程

---

## ✅ 总结

完成以上安装后，你的本地开发环境就搭建好了！

**必需安装**：
- ✅ Node.js
- ✅ pnpm
- ✅ Git
- ✅ VS Code
- ✅ PostgreSQL

**推荐安装**：
- ⚠️ Docker Desktop（简化数据库管理）
- ⚠️ Redis（性能优化）

祝你开发顺利！🎉

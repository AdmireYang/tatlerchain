# ===========================================
# 环境变量配置模板
# 复制此文件为 .env 并填写实际值
# 注意：.env 文件包含敏感信息，禁止提交到 Git！
# ===========================================

# ===========================================
# 域名配置（HTTPS 必填）
# ===========================================

# 你的域名（不含 http/https）
DOMAIN_NAME=tatlerchain.com

# ===========================================
# 数据库配置
# ===========================================

DB_USER=tatlerchain
DB_PASSWORD=your_password_here
DB_NAME=tatlerchain
DB_PORT=5432

# ===========================================
# API 配置
# ===========================================

API_PORT=3001
# HTTPS 启用后，这里应该改为 https://
API_BASE_URL=https://your-domain.com/api

# ===========================================
# Web / Admin 配置
# ===========================================

WEB_PORT=3003
ADMIN_PORT=8080
ADMIN_INTERNAL_PORT=8081

# ===========================================
# JWT 配置
# ===========================================

JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# ===========================================
# 文件上传配置
# ===========================================

# 存储类型：local（本地存储）或 oss（阿里云 OSS）
UPLOAD_STORAGE=local

# ===========================================
# 阿里云 OSS 配置（当 UPLOAD_STORAGE=oss 时必填）
# ===========================================

# OSS 地域，如：oss-cn-hangzhou、oss-cn-shanghai
OSS_REGION=oss-cn-shanghai

# AccessKey ID（建议使用 RAM 子账号，不要用主账号！）
OSS_ACCESS_KEY_ID=your_access_key_id

# AccessKey Secret
OSS_ACCESS_KEY_SECRET=your_access_key_secret

# OSS Bucket 名称（仅填 bucket 名，不含域名）
OSS_BUCKET=your_bucket_name

# CDN 域名（可选，不填则用 OSS 默认域名）
OSS_CDN_DOMAIN=
